# Uclan-shop

## Daria Golubeva G-number: 21306382

### Work completed in accordance with the Marking Criterion:
 1. **Contains a homepage (`index.html`)**  
Page `index.html ` created correctly.  
Includes a welcome message and media content: local video and YouTube `<iframe>`.  
The structure `header`, `main`, `section`, and `footer` is used.

2. **Proper use of HTML**  
HTML5 semantic markup is applied on all pages.  
The content is organized into three main sections: `header`, `main`, and `footer`.  
Correct headings (`h2`, `h3`, `h4`) and paragraphs (`p`) are used.

3. **Proper use of CSS**  
It is styled the same on all pages.  
Selectors of classes, elements, and media queries are used.  
The colors are applied according to the UCLan brand palette.

4. **Video demonstration**  
A video demonstration of the website was recorded (max. 3 minutes), including functionality and code.

5. **Laboratory demonstration**  
A laboratory demonstration was conducted with a demonstration of all the functions.

6. **Clear and commented code**  
The HTML, CSS, and JS code are readable, with comments for the main functions and elements.

7. **Homepage passes validation**  
HTML and CSS on `index.html ` verified through the W3C Validator.

8. **Basic Product Page**  
On `products.html ` 9 products with attributes are displayed: name, color, price, stock availability, description, image.  
The product cards are stylized.  
The statuses “in stock“, ”few left", and “out of stock" visually differ.

9. **Clear and varied navigation**  
The main menu contains links to Home, Products, Cart.  
The submenu through the product cards (“View more") leads to the Item page.  
Adaptive menu for mobile devices (hamburger menu) is implemented.

10. **Responsive design**  
CSS Grid and Flexbox are used for adaptive layout.  
Product cards and page content adjust to the width of the screen.

11. **Advanced Products page**  
JavaScript renders all the products from the array.  
Filtering by availability status works.  
The "View more” function opens the Item page.

12. **Basic Item page**  
The Item page is only available through Products and Cart.  
The `sessionStorage` is used to transfer the selected product.  
The product information is displayed correctly.

13. **Mobile Navigation**  
The menu is adapted for mobile devices.  
Hamburger menu opens and closes correctly.

14. **Basic shopping cart**  
The Cart page shows all the added products.  
Quantity, color, image and price are displayed.  
The user can change the quantity, delete items, and empty the shopping cart.

15. **Advanced Item page**  
The Item page uses `localStorage` to add items to the cart.  
The "Add to Cart“ button adds the selected product to the cart.

16. **Semantic elements of HTML5**  
The following elements are used: `header`, `main`, `section`, `footer`, `article`, `nav`.  
The use of semantics is commented out in the code.

17. **Advanced Shopping Cart**  
Implemented the use of coupons (`STUDENTFEST10 = 10%`, `UCLANDISCOUNT20 = 20%`).  
Changing the quantity, deleting items, and clearing the shopping cart are implemented.  
The amount is recalculated after the changes.

18. **Website is fully validated**  
All pages have passed HTML and CSS validation.  
WCAG principles are followed (alt-texts, semantic markup, color contrast).

19. **Using a version control system with regular commits**  
The project is hosted on GitHub.  
Regular commits reflect the progress of the work.

20. **Professional appearance and absence of significant usability errors**  
The style of the website is uniform, modern and readable.  
Minimal usability errors; the site is adapted for mobile devices.  
The colors, fonts, and navigation meet the brand's standards and user experience.